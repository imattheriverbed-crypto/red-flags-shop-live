# Red Flags & Receipts (Minimal Skeleton)

This is a minimal clean skeleton of the Red Flags frontend so you can initialize a repo and push without large/generated files.

To run locally:

1. Install dependencies: npm install
2. Start dev server: npm run dev

The original full project contained a Base44 integration and more dependencies; recreate or restore additional files if needed.
